import 'package:flutter/material.dart';

class ColorUtils {
  static const primaryColor = Color(0xff004E00);
  static const black = Colors.black;
  static const grey = Color(0xFFF2F3F2);
  static const Color login = Color(0xff0072BB);
  static const Color Texfieldtext = Color(0xff1616164d);

  static const appGrediant = LinearGradient(
    colors: [Color(0xFF009BDF), Color(0xFF0072BB)],
    begin: FractionalOffset.topCenter,
    end: FractionalOffset.bottomCenter,
  );

// static const grey
}
